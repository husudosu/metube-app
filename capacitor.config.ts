import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "com.husudosu.metube",
    appName: "metube",
    webDir: "dist",
    bundledWebRuntime: false,
};

export default config;
